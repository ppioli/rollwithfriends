import { CSSProperties } from "react";

export const layerContainer: CSSProperties = {
  position: "relative",
  touchAction: "none",
  overflow: "show",
  width: "100%",
  height: "100%",
};

export const layer: CSSProperties = {
  position: "absolute",
  top: 0,
  left: 0,
};
